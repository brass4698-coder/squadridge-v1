/**
 * Backwards-compat re-export. Historical home for the scripted demo persona.
 * Real source of truth lives in `demoScenarios.ts`, which now also defines two
 * additional scenarios beyond cross-border corridor.
 */
export { DEMO_PERSONA } from './demoScenarios';
