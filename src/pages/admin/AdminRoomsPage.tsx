import { ModDashboardPage } from '../ModDashboardPage';

/**
 * Full moderation room and message view — same as legacy `/mod` route.
 */
export function AdminRoomsPage() {
  return <ModDashboardPage />;
}
