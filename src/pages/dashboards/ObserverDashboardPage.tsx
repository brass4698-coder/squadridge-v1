import { Navigate } from 'react-router-dom';

/** Observer product landing → executive governance view. */
export function ObserverDashboardPage() {
  return <Navigate to="/app/executive" replace />;
}
