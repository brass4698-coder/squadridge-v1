# Placeholder environment variables for vitest test runs.
# Real Supabase credentials are NOT required; tests that need a live client use in-test stubs/mocks.
# This file is safe to commit — values are non-functional placeholders only.
VITE_SUPABASE_URL=https://placeholder-test.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=test-placeholder-publishable-key
