# Test environment — placeholder values so the Supabase singleton in
# src/utils/supabase.ts can be imported without throwing "supabaseUrl is required".
# These are NOT real credentials; tests that need a working Supabase client
# provide their own stub via createSupabaseMessagesStub or vi.mock.
VITE_SUPABASE_URL=https://placeholder-test.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-anon-key-for-tests
