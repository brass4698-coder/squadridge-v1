# This workflow was a GitHub template placeholder and has been removed.
# SquadRidge Edge Functions use the Supabase CLI Deno runtime, deployed via
# deploy-supabase-production.yml — not a standalone Deno project.
name: _removed-deno-placeholder
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is intentionally disabled. See deploy-supabase-production.yml.'
