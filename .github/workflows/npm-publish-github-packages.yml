# This workflow was a GitHub template placeholder and has been removed.
# MENDguild is a private app ("private": true in package.json) — not a published npm package.
# Release artifacts are deployed via deploy-frontend.yml to Vercel/Netlify.
name: _removed-npm-publish-placeholder
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is intentionally disabled. See deploy-frontend.yml.'
