# This workflow was a GitHub template placeholder and has been removed.
# SquadRidge is a Vite/React SPA — not a Jekyll site.
# All CI is handled in ci.yml.
# This file is kept as a stub so the filename does not reappear via GitHub template suggestions.
name: _removed-jekyll-placeholder
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is intentionally disabled. See ci.yml.'
