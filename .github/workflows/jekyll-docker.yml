# DELETED — was an unmodified GitHub starter template never adapted to SquadRidge.
# Deployments are handled by deploy-supabase-production.yml and deploy-frontend.yml.
# This file is intentionally empty and will be removed in the next cleanup pass.
# @see chore(workflows): delete stale GitHub template stubs
